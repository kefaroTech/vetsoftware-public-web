/**
 * Las mismas afirmaciones de existencia que usan las pruebas unitarias, para no
 * escribirlas dos veces. Ver `tests/helpers/exigir.ts`: comprueban de verdad y
 * fallan diciendo qué faltaba, en lugar de `!`, que no comprueba nada.
 */
export { elemento, exigir } from '../../tests/helpers/exigir'
